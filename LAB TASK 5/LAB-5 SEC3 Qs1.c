#include <stdio.h>
int main()
{
    int num1,num2;
    printf("enter two numbers");
    scanf("%d",&num1);
    scanf("%d",&num2);
    (num1/2 > num2/2) ? printf("number 1 is maximum"):printf("number 2 is maximum");
     

    return 0;
}